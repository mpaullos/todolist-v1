<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ToDo-List</title>
  </head>
  <body>
    <% if (kindOfDay === "Sábadp" || kindOfDay == "Domingo") { %>

    <h1 style="color: red">Hoje é <%= kindOfDay %></h1>
    <% } else { %>
    <h1 style="color: purple">Hoje é <%= kindOfDay %></h1>
    <% } %>
  </body>
</html>
